<!DOCTYPE html>
<html>
  <head>
    <title>Employee Detail</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="mb-4">Employee Detail</h1>
      <a href="/employees" class="btn btn-secondary mb-3">← Back to List</a>

      <table class="table table-bordered">
        <tbody>
          <tr>
            <th>Name</th>
            <td><%= employee.name %></td>
          </tr>
          <tr>
            <th>Address</th>
            <td><%= employee.address %></td>
          </tr>
          <tr>
            <th>Position</th>
            <td><%= employee.position %></td>
          </tr>
          <tr>
            <th>Salary</th>
            <td>$<%= employee.salary %>/year</td>
          </tr>
        </tbody>
      </table>

      <a href="/employees/edit/<%= employee._id %>" class="btn btn-primary">Edit</a>
      <form action="/employees/delete/<%= employee._id %>" method="post" class="d-inline">
        <button type="submit" class="btn btn-danger">Delete</button>
      </form>
    </div>
  </body>
</html>
